# S5Max 两条老板验收示范片设计

**状态：** 已批准并完成快速预览版  
**日期：** 2026-08-07  
**方案：** A（数据验证片 + 科技质感片）  
**验收基准：** 现有 `out/4-27-scale.mp4` 的字幕与画面包装水平，新增 AI 中文配音

## 1. 目标

先交付两条可供老板判断方向的 S5Max 竖屏示范片。老板通过后再建设全量视觉素材库；本轮不实现通用 TTS、批量任务、共享代理缓存或全库索引。

## 2. 数据依据

- 示范片 01 使用 CSV 视频 ID `7670081275056046122` 的原文。该脚本现有 3 日点击率为 12.12%，销售额为 0；只能称为当前点击表现最高脚本，不能称为转化冠军。
- 示范片 02 使用 CSV 视频 ID `7670835392590872618` 的原文。该行没有可用效果数据，作用是验证科技质感方向。
- 字幕保持 CSV 原文；AI 配音只把 `S5Max`、`39000`、`Type-C` 转成更自然的发音写法，不改变含义。

## 3. 两条结构

| 片号 | 时长 | 镜头顺序 | CTA |
|---|---:|---|---|
| 01 数据验证片 | 28.5 秒 | 红布揭晓 → 真人剃须 → 转子 → 入水 → Type-C → 手持产品 | 进直播间看看，入手不亏。 |
| 02 科技质感片 | 24.6 秒 | 转台 → 电子屏/充电 → 机身扫光 → 转子 → 冲水 → 礼盒产品 | 进直播间看更多细节 |

## 4. 音频与剪辑

- 本机 macOS `Tingting` 普通话语音，语速 `230`。
- 示范片 01 配音实测 27.895 秒；示范片 02 配音实测 24.131 秒。
- 无 BGM，源素材音轨静音；最终仅保留单声道 AAC 人声。
- 切点根据逐句 TTS 时长校正，字幕按同一语义段落显示。

## 5. 技术规格

- MP4、H.264、`yuv420p`、1080×1920、30fps、AAC。
- 01 为 855 帧；02 为 738 帧。
- SMB 素材始终只读；代理、Props、音频、联系表和成片均位于本地仓库。
- CTA 必须在首帧和末帧保持商品可辨，不得落到空桌面。

## 6. 老板验收包

- `out/s5max-demo-01-data.mp4`
- `out/s5max-demo-02-tech.mp4`
- `work/s5max-quick/contacts/demo-01-final-cut.jpg`
- `work/s5max-quick/contacts/demo-02-final-cut.jpg`

## 7. 本轮边界

- 这是快速方向验证，不代表已完成全量视觉建库。
- 未加入 BGM、真人配音、云端高拟真 TTS 或自动语音对齐。
- 技术与视觉检查由 Codex 完成；配音音色与发音仍需老板或人工监听确认。
