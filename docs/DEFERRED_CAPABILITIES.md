# 尚未接入的能力

本文件是后续实现清单。框架阶段不会用随机数据或静默回退假装这些能力已经可用；调用未配置接口时必须返回 `not_configured` 并说明缺少的配置。

## NAS 素材接入

### 已实现：SMB 可生产基础资产库

`scripts/asset-library.mjs scan` 已提供只读全量扫描、ffprobe 元数据、快速指纹、目录标签、固定联系表、CTA 四帧图、质量提示、断点 checkpoint 和本地检索。所有产物写入 `work/asset-library/`，全量代理仍按需生成。

### 仍需真实挂载验收

**需要提供**

- 工作站上的稳定 NAS 挂载路径。
- 原始视频、授权音乐和输出目录约定。
- 是否允许重命名、移动或生成相邻代理文件。

**后续输出（基础库之外）**

- 视觉 AI 语义标签、人工修正标签、向量相似检索和后台任务队列（仅在基础库数据证明需要时增加）。
- NAS 断连检测、断点续扫和路径重新映射。

**完成标准**

- 未改变的文件不会重复处理；新增、移动、损坏文件均有明确状态；真实 `/Volumes/192.168.50.79` 全量运行后通过人工视觉抽查。

## 商品目录导入

**需要提供**

- 真实 Excel/CSV 样例。
- SKU、商品名、卖点、别名和参考图片的列映射。

**后续输出**

- 可校验、可重复导入的商品数据及逐行错误报告。

**完成标准**

- 同一 SKU 可以稳定关联资料表和素材镜头。

## 素材切镜与索引

**需要提供**

- 真实素材样本及可接受的初次索引时间。
- 工作站的可用磁盘空间和代理文件质量要求。

**后续输出**

- FFmpeg/ffprobe 元数据、镜头边界、关键帧、缩略图和低清代理。
- 商品、场景、动作、景别、画质、卖点与置信度标签。

**完成标准**

- Codex 可以按 SKU 和卖点召回带准确时间区间的候选镜头。

## Codex 三候选剪辑

**需要提供**

- 首个真实 SKU、脚本文件和对应素材。
- 商品画面误配的具体判定样例。

**后续输出**

- 钩子、卖点和 CTA 脚本分段。
- 三份不同镜头与节奏的 `Timeline` JSON。
- 低置信度镜头的人工确认清单。

**完成标准**

- 三份草稿均为 20–40 秒，并优先保证商品和卖点准确。

## 云端 TTS 与音频对齐

**需要提供**

- TTS 服务商、API 凭证、目标音色和商用授权确认。
- 旁白语速、停顿和音量偏好。

**后续输出**

- 旁白音频、句级或词级时间戳及可恢复的失败状态。
- 未配置云端服务时，可显式选择 macOS 本机语音作为开发回退。

**完成标准**

- 旁白、字幕和镜头时长保持同步，失败不会产生伪音频。

## 音乐库与混音

**需要提供**

- NAS 授权音乐目录及风格、BPM、情绪等已有元数据。

**后续输出**

- 音乐检索、裁切、淡入淡出、旁白避让和音量标准化。

**完成标准**

- 导出视频中旁白清晰，音乐来源与授权信息可追溯。

## 后台任务与审核编辑

**需要提供**

- 真实日任务峰值、可接受预览等待时间和保留策略。

**后续输出**

- 可恢复的扫描、分析、配音、预览和正式渲染任务。
- 网页镜头替换、入出点、字幕、音乐修改和局部重渲染。
- H.264/AAC MP4 与 UTF-8 SRT 导出。

**完成标准**

- 任一任务失败后可定位原因并重试，不丢失已审核的时间线。
